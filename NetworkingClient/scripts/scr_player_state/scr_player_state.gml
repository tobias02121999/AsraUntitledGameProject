// Run the current player state
var state = argument0;

switch (state)
{
	case "OVERWORLD":
		// Run the player overworld functions
		scr_player_move(); // Move the player based on the input
		break;
}
