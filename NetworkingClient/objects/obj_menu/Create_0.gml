// Initialize the menu object
buttonID[0] = 0;
buttonID[1] = 1;
buttonAmount = array_length_1d(buttonID);
spacing = 40;
xPos = room_width / 2;
yPos = room_height / 2;
selected = 0;

// Get the button data
scr_menu_buttonData();	
