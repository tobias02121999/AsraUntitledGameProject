// Initialize the prop object

// Get the prop statistics
scr_prop_statistics();

// Initialize the prop system variables
angle = 0; // The props current angle
angleTarget = 0; // The props target angle to turn towards
shadowOffset = 0; // The Y offset of where to draw the props shadow
