// Initialize the enemy statistics

// Define the enemy statistics variables
dodgeSpeed = 1;
dodgeArea = 100;
maxHp = 10; // The maximum enemy hp
fireballAlarmMin = 10;
fireballAlarmMax = 100;
fireballAmmo = 3; // The amount of fireballs the enemy can shoot in one attack
