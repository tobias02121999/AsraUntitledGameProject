// Initialize the projectile

// Initialize the projectile system variables
isFriendly = false; // Keeps track of if the projectile is friendly or hostile

// Initialize the projectile statistics variables
scr_projectile_statistics();
