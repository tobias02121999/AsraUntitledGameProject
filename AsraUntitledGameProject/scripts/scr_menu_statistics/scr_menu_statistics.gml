// Initialize the menu statistics

// Define the menu statistics variables
menuPosX = 0; // The X position of the menu
menuPosY = 0; // The Y position of the menu

textOffsetX = 6; // The text offset from its X position on the button
textOffsetY = -1; // The text offset from its Y position on the button

fontSize = 1.5; // The size of the text font displayed on the buttons

buttonAmount = 0; // The amount of buttons to instantiate
buttonDistanceX = 0; // The distance between the buttons on the X axis
buttonDistanceY = 16; // The distance between the buttons on the Y axis

buttonSprite[0] = spr_button; // The sprite of the button
buttonID[0] = 0; // The script of the button
buttonText[0] = "none"; // The text displayed on the button
