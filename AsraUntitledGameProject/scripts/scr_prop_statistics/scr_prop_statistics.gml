// Initialize the prop statistics

// Define the prop statistics variables
catchWind = false; // If the prop should catch wind or not
angleSpeed = .05; // The speed to turn towards the props target angle
windStrength = 2.5; // The current strength of the wind (at all times)
