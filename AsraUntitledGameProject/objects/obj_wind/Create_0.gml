// Inherit the parent event
event_inherited();

// Initialize the wind object
triggerScript = scr_trigger_wind; // This script will be ran whenever collision with the trigger is detected
spd = 2; // The current speed of the wind
strength = .725; // The current strength of the wind
