// Get the user input
scr_player_getInput(playerID);

// Run the player state
scr_player_states();

// Disable the player in the main menu
visible = (room != rm_main)

// Control the depth
depth = -(y + 20)
